name: curl
category: utils
version: "7.70.0+2"
requires:
- name: musl
  category: system
  version: ">=0"
- name: libressl
  category: libs
  version: ">=0"
- name: zlib
  category: libs
  version: ">=0"

labels:
  github.repo: "curl"
  github.owner: "curl"

uri:
- https://curl.haxx.se
license: "curl"
